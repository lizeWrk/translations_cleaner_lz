# Example

```sh
# Add translations_cleaner_lz as a dev dependency
dart pub add --dev translations_cleaner_lz

# Run the command to clean all the translations
flutter pub run translations_cleaner_lz
```
